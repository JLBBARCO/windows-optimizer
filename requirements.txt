# Runtime: the application uses only the Python standard library.
# The packages below are optional and only used by lib/json.read_external_json();
# when they are missing the code falls back gracefully.
certifi==2026.7.22
charset-normalizer==3.5.1
idna==3.19
requests==2.34.2
urllib3==2.7.0
